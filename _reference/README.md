# _reference

Terraform 구성에 **포함되지 않는** 참고 자료입니다.
(하위 디렉터리이고 확장자도 `.tf` 가 아니므로 루트 모듈이 읽지 않습니다.)

| 파일 | 내용 |
| --- | --- |
| `ex-net-network.ian.tf.txt` | 예전 `main.tf-` 백업. `ian_*` 네이밍으로 작성한 ex-net 네트워크 구성의 초기 버전 |
| `test-count-foreach-examples.tf.txt` | 예전 `test.tf`. 전부 주석 처리돼 있던 `count` / `for_each` 실습 메모 |
